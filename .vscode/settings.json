{
    "python.analysis.extraPaths": [
        "${workspaceFolder}",
        "${workspaceFolder}/..",
        "${workspaceFolder}/../prot"
    ],
    "python.autoComplete.extraPaths": [
        "${workspaceFolder}",
        "${workspaceFolder}/..",
        "${workspaceFolder}/../prot"
    ]
}
